# --- core (required) ---
pyyaml>=6.0
numpy>=1.24
rank-bm25>=0.2.2
python-dotenv>=1.0
google-genai>=1.0.0        # Gemini embeddings + generation

# --- optional ---
# redshift-connector>=2.1.0  # live Redshift introspection (add-table) + EXPLAIN validation
# rich>=13.0                 # prettier CLI output
